{
  "name": "test-hw",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "install:api": "cd api && npm install",
    "install:app": "cd app && npm install",
    "install:all": "npm-run-all --parallel install:api install:app",
    "start:api": "cd api && npm run dev",
    "start:app": "cd app && npm run dev",
    "start:all": "npm-run-all --parallel start:api start:app"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
